from django.apps import AppConfig


class TaskCodeExecConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.task_code_exec"
    verbose_name = "Задания: Выполнение кода"
